@model IEnumerable<YogamedAppRole.Models.UserDisease>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserIdFk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disease)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiseaseIdfk)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserIdFk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disease)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiseaseIdfk)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Udid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Udid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Udid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
